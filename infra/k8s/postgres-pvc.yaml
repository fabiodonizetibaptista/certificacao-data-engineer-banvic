apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: banvic
  labels:
    app: postgres
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 2Gi
